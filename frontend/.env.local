# Copy to .env.local and fill in values before running

# Backend API Gateway base URL (no trailing slash)
NEXT_PUBLIC_API_URL=https://fabwj4hz0g.execute-api.us-east-1.amazonaws.com/prod

# Coach ID (single-coach deployment)
NEXT_PUBLIC_COACH_ID=coach-001
